# Stardew i18n Translator v1.1.1

This hotfix adds useful local diagnostics for bug reports.

- The app writes a rotating, size-capped log under `Data/logs/`.
- **Settings → About → Open logs folder** opens the folder directly.
- Logs are never uploaded automatically, but they may contain local paths; remove
  private information before attaching one to an issue.